# دليل تشغيل وبناء تطبيق الأندرويد | المديرية العامة للموانئ
## Android Application & Cloud Build Guide

تم تجهيز منظومة متابعة الخطط والإنجاز اليومي لتعمل كتطبيق هاتف محمول أندرويد أصلي (**Native Android App**) باستخدام محرك **Capacitor** ومسارات البناء المؤتمتة عبر السحاب دون الحاجة لتثبيت بيئة Android Studio على جهازك.

---

## 🌟 1. الميزات والتجهيزات التي تمت إضافتها

1. **شاشة افتتاحية رسمية (Splash Screen):**
   - ظهور شعار الجمهورية العربية السورية الذهبي الخاص بالمديرية مع خلفية زيتي ملكي (`#0c3e35`).
2. **شريط الحالة المندمج (Status Bar):**
   - تلوين شريط البطارية والتنبيهات العلوي ليتطابق مع هوية المنظومة.
3. **دعم هوامش الشاشة الكاملة (Safe-Area & Notch):**
   - حماية الواجهة من التداخل مع الكاميرا الأمامية (Notch) أو شريط إيماءات أندرويد السفلي.
4. **تفاعل لمسي سلس (Native Touch Feel):**
   - إلغاء وميض الروابط في المتصفح ومنع التكبير والتصغير العرضي المزدوج لتجربة تطبيق حقيقي.
5. **مرونة الاتصال بالسيرفر (Dynamic API URL):**
   - إمكانية ضبط عنوان السيرفر من داخل التطبيق أو من إعدادات البناء دون الحاجة لإعادة كتابة الكود.
6. **نظام إشعارات الموبايل الفورية الشبيه بالواتساب (Native Heads-up Push Notifications):**
   - ظهور إشعار فوري في شريط الهاتف العلوي (Notification Drawer / Lock Screen) لحظة صدور أي تعميم إداري أو تكليف رسمي.
   - تنبيه صوتي واهتزاز عالي الأهمية (High Priority Channels).
   - النقر على الإشعار يفتح التطبيق مباشرة ويعرض نص وتفاصيل التعميم الكاملة مع إمكانية توثيق الاطلاع.
   - دعم أذونات أندرويد 13 فما فوق (`POST_NOTIFICATIONS`).
---

## 🚀 2. استخراج ملف الـ APK بضغطة زر واحدة (بدون Android Studio)

تم إنشاء مسار بناء مؤتمت في **GitHub Actions** يقوم بإنشاء ملف الـ `APK` على خوادم GitHub السحابية مجاناً:

### الخطوات:
1. اذهب إلى مستودع المشروع على **GitHub**.
2. اضغط على تبويب **Actions** من القائمة العلوية.
3. من القائمة الجانبية اليسرى، اختر سير العمل: **`Build Android APK`**.
4. اضغط على زر **Run workflow** الأزرق في اليمين:
   * **رابط السيرفر (`server_url`):** ضع عنوان الـ IP أو الدومين الخاص بالخادم الخلفي (مثلاً: `http://192.168.1.50:4000` أو `https://ports-api.yourdomain.sy`).
   * **نوع النسخة (`build_type`):** اختر `debug` للتجربة السريعة والتثبيت المباشر، أو `release` للنشر.
5. اضغط على **Run workflow**.
6. انتظر من 2 إلى 3 دقائق حتى يكتمل البناء، ثم اضغط على التشغيل المكتمل.
7. ستجد في قسم **Artifacts** في الأسفل ملف باسم: **`ports-daily-system-debug.apk`**.
8. قم بتنزيله وإرساله إلى الهاتف عبر الواتساب، التيليغرام، أو عبر كابل الـ USB وتثبيته فوراً!

---

## ⚙️ 3. كيفية تغيير رابط السيرفر من داخل التطبيق المحمول

إذا قمت بتثبيت التطبيق وتريد توجيهه إلى خادم داخلي أو خارجي في أي وقت:
* يمكنك فتح أدوات التخزين أو استخدام وحدة التحكم وكتابة:
  ```javascript
  localStorage.setItem('ports_custom_api_url', 'http://YOUR_SERVER_IP:4000');
  ```
  وسيقوم التطبيق بالاتصال فوراً بالسيرفر المحدد دون الحاجة لإعادة تثبيت التطبيق.

---

## 💻 4. البناء المحلي (للمطورين عبر سطر الأوامر)

إذا رغبت في بناء التطبيق محلياً على جهازك دون الحاجة لفتح واجهة Android Studio:

### المتطلبات:
* تثبيت **Java JDK 17** والتأكد من ضبط متغير البيئة `JAVA_HOME`.
* تثبيت **Android Command Line Tools** أو **Android SDK**.

### أوامر البناء:
```bash
# 1. الدخول لمجلد الواجهة وبناء Next.js
cd client
npm run build

# 2. مزامنة ملفات الـ Android
npx cap sync android

# 3. بناء ملف الـ APK بواسطة Gradle مباشرة
cd android
./gradlew assembleDebug
```
ستجد الملف الناتج في المسار:
`client/android/app/build/outputs/apk/debug/app-debug.apk`

---

## 📱 5. خيار الـ PWA السريع (التثبيت المباشر من المتصفح)

تم تجهيز ملف `manifest.json` وأيقونات المنظومة، لذلك يمكن لأي مستخدم يفتح النظام من متصفح الهاتف (Chrome / Edge / Safari) أن يضغط على خيارات المتصفح ثم:
* **"إضافة إلى الشاشة الرئيسية" (Add to Home Screen)** أو **"تثبيت التطبيق" (Install App)**.
* سيظهر التطبيق على شاشة الهاتف كأيقونة مستقلة تفتح بملء الشاشة دون شريط عناوين المتصفح.
